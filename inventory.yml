---
plugin: gcp_compute

keyed_groups:
  - key: labels
    prefix: label

projects:
  - sinuous-tuner-414107

auth_kind: serviceaccount

service_account_file: /home/gagguturuudaykamalbabu/ansible-dir/keys.json 

compose:
  ansible_user: gagguturuudaykamalbabu
  ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

groups:
  remote: "'dev' in (labels|list)"
