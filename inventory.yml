---
plugin: gcp_compute

keyed_groups:
  - key: labels
    prefix: label

projects:
  - sinuous-tuner-414107

auth_kind: serviceaccount

compose:
  ansible_user: sa_114966270712069978938
  ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

groups:
  remote: "'dev' in (labels|list)"
